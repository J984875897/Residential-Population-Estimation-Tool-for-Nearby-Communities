# ============================================================
# 城市配置
# CITY_CODE: 贝壳城市代码（域名前缀），如 lz（兰州）、bj（北京）、sh（上海）、cd（成都）
# CITY_NAME: 城市中文名，用于输出文件名和日志标题
# ============================================================
CITY_CODE = "lz"
CITY_NAME = "兰州"
BASE_URL = f"https://{CITY_CODE}.ke.com"

# ============================================================
# 目标地点坐标
# 复制本文件为 config.py 后，替换为自己的目标地点信息。
# 请勿把真实项目名称、坐标或客户信息提交到公开仓库。
# 小区坐标来自贝壳页面；程序直接使用 Haversine 公式计算距离，不做坐标系转换。
# 请尽量让目标坐标与贝壳页面坐标处于同一坐标体系，否则边界小区可能有偏差。
# ============================================================
TARGET_LNG = 103.000000
TARGET_LAT = 36.000000
TARGET_NAME = "示例目标地点"

RADIUS_KM = 3.0

# 目标城市的行政区拼音（贝壳 URL 使用拼音格式，可通过 GUI「自动获取行政区」按钮发现）
SEARCH_DISTRICTS = ["qilihequ", "anningqu"]

# 请求延时（秒），在 MIN~MAX 之间随机，防止触发反爬
DELAY_MIN = 2.0
DELAY_MAX = 4.5

# 当页面无法提取户型分布时使用的默认平均室数
DEFAULT_B = 2.0

# 人口估算系数
PEOPLE_PER_ROOM = 1.5
OCCUPANCY_RATE = 0.6
